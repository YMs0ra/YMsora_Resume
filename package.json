{
  "name": "ymsora-blog-resume",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@astrojs/cloudflare": "12",
    "astro": "^5.8.1"
  },
  "devDependencies": {
    "wrangler": "^4.106.0"
  }
}
